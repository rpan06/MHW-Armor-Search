const start = require('');
const getDecorationInfo = require('');
const handleInput = require('');
const handleDecorations = require('');
const handleSubmit = require('');
const getStats = require('');
const displayResults = require('');

function sum(a, b) {
    return a + b;
  }

test('start function retrieves an array', () => {
    expect(sum(1, 2)).toBe(3);
});

test('start function parses head,body,waist,armor,legs, and charms into variables', () => {
    expect(sum(1, 2)).toBe(3);
});

test('start function renders aforementioned variables into dropboxes', () => {
    expect(sum(1, 2)).toBe(3);
});

test('getDecorationInfo retrieves an array', () => {
    expect(sum(1, 2)).toBe(3);
});

test('getDecorationInfo function parses head,body,waist,armor,legs, and charms into variables', () => {
    expect(sum(1, 2)).toBe(3);
});

test('getDecorationInfo function renders aforementioned variables into dropbox', () => {
    expect(sum(1, 2)).toBe(3);
});


test('handleInput detects change in armor selection', () => {
    expect(sum(1, 2)).toBe(3);
});

test('handleInput detects if armor has slots, then renders available gems to dropbox', () => {
    expect(sum(1, 2)).toBe(3);
});

test('handleDecorations does... stuff', () => {
    expect(sum(1, 2)).toBe(3);
});

test('handleSubmit recieves input from form', () => {
    expect(sum(1, 2)).toBe(3);
});

test('getStats recieves input from handleSubmit', () => {
    expect(sum(1, 2)).toBe(3);
});

test('getStats returns stats of associated armor', () => {
    expect(sum(1, 2)).toBe(3);
});

test('getStats returns stats in right format', () => {
    expect(sum(1, 2)).toBe(3);
});

test('display results receives the right armor', () => {
    expect(sum(1, 2)).toBe(3);
});

test('getStats returns the right stats', () => {
    expect(sum(1, 2)).toBe(3);
});

